name: CI
on:
  pull_request:
  push:
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        java:
          - 8
        scala:
          - 2_12
          - 2_13
          - 3
        include:
          - java: 17
            scala: 2_13
          - java: 17
            scala: 3
    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-java@v2
      with:
        java-version: ${{matrix.java}}
        distribution: adopt
    - uses: coursier/cache-action@v6
    - run: sbt "SetScala${{ matrix.scala }}" Test/compile
